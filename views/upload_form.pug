doctype html
html
    head
        title classifier form
    body
        form(action='/classify', method='POST', enctype='multipart/form-data')
            input(type='file', name='image')
            br
            button(type='submit') classify